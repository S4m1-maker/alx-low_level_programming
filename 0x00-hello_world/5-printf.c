#include <stdio.h>

/**
 * main - main function is the parent function
 *
 * Return: returns the value 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
