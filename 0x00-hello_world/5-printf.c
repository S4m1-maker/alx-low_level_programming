#include <stdio.h>

/**
 * main - prints a string by calling the printf function
 *
 * Return: returns 0 when successful
 */

int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");

		return (0);
}
