#include "main.h"

/**
* mul - multiplies two integers
* @a: is the first argument of the function
* @b: is the second argument of the function
*
* Return: returns an integer when successful
*/

int mul(int a, int b)
{
	int ans = a * b;

	return (ans);
}
